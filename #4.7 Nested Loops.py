#outer loop will iterate 60 times
for minutes in range(60):

    #each time, this loop will iterate 60 times.
    for seconds in range(60):
        print(minutes, ':', seconds)

#for every time the minute loop iterates, the seconds loop will iterate 60 times.
